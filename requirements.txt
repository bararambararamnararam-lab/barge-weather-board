# 이 프로그램이 쓰는 파이썬 부품 목록입니다.
# 0_처음설치.bat 이 이 파일을 보고 자동으로 다 깔아 줍니다.

requests>=2.31          # 인터넷에서 자료 받아오기 (기상청·NOAA·met.no)
PyYAML>=6.0             # 설정 파일(weather_config.yaml) 읽기
pandas>=2.0             # 표 계산
streamlit>=1.36         # PC용 화면 (2_화면열기.bat)
python-dotenv>=1.0      # .env 에 넣어 둔 기상청 API 키 읽기
openpyxl>=3.1           # 엑셀 파일 만들기

# 아래 둘은 유럽중기예보센터(ECMWF) 자료를 받고 푸는 데 씁니다.
# 기상 자료는 GRIB2 라는 압축 형식이라 전용 해독기가 있어야 읽힙니다.
ecmwf-opendata>=0.3.19  # ECMWF 무료 공개자료 내려받기
eccodes>=2.38           # GRIB2 해독 (NOAA 자료도 같은 형식)
